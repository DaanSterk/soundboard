<%= form_for @board do |board| %>
    <div class="field">
      <%= board.label :name %>:
      <%= board.text_field :name %><br />
    </div>

    <div class="field">
      <%= board.submit :class => 'btn btn-success' %>
    </div>
<% end %>